export interface workOrder {
    id?: string;
    dispatcherId: string;
    customerId: string;
    farmId: string;
    fieldId: string;
    service: string,
    tractorDriver: string,
    fieldAddress: string,
    phone: string,
}